Config = {}

-- URL do seu Dashboard (sem a barra final)
-- Ex: "https://seu-painel.com" ou "http://localhost:3000" para testes locais
Config.DashboardURL = "http://localhost:3000"

-- O Token Secreto do seu servidor (Deve ser gerado no Painel Admin)
Config.SecretToken = "CHANGE_ME_IN_ADMIN_PANEL"

-- Intervalo de atualização de jogadores online (em milissegundos)
-- 60000 = 60 segundos
Config.UpdateInterval = 60000

-- Ativar logs no console do servidor (debug)
Config.Debug = true
