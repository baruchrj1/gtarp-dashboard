# ==========================================
# GTARP Dashboard - Environment Variables
# ==========================================
#
# ==========================================
# SUPABASE CONFIGURATION (Central de Tenants)
# ==========================================
# URL e Chaves do projeto Supabase (gtarp-tenants)
# Crie em: https://supabase.com/dashboard
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
#
# ==========================================
# DATABASE (Fallback para backward compatibility)
# ==========================================
# Ainda usado como fallback quando Supabase não estiver configurado
# Obtenha isso em: Vercel > Storage > Postgres > .env.local tab
# Ou use Prisma Postgres Accelerate
PRISMA_DATABASE_URL="sua-connection-string-aqui"
POSTGRES_URL="sua-direct-connection-string-aqui"
#
# === NEXTAUTH ===
# URL do site (SEM barra no final)
NEXTAUTH_URL="http://localhost:3000"
#
# Gere com: openssl rand -base64 32
NEXTAUTH_SECRET="gere-um-secret-unico-aqui"
#
# ==========================================
# NOTA: Com Supabase configurado, credenciais Discord
# são lidas dinamicamente do banco de dados.
# ==========================================

# 
# ARQUITETURA MULTI-TENANT:
# As credenciais Discord (Client ID, Secret, Guild ID, Roles) são lidas
# diretamente do banco de dados (tabela Tenant).
# 
# Use o gtarp-manager para configurar cada cliente/tenant.
# ==========================================

# === DATABASE (Vercel Postgres) ===
# Obtenha isso em: Vercel > Storage > Postgres > .env.local tab
# Ou use Prisma Postgres Accelerate
PRISMA_DATABASE_URL="sua-connection-string-aqui"
POSTGRES_URL="sua-direct-connection-string-aqui"

# === NEXTAUTH ===
# URL do site (SEM barra no final)
NEXTAUTH_URL="http://localhost:3000"

# Gere com: openssl rand -base64 32
NEXTAUTH_SECRET="gere-um-secret-unico-aqui"

# ==========================================
# NOTA: Credenciais Discord NÃO são mais necessárias aqui!
# Elas são lidas dinamicamente do banco de dados para cada tenant.
# 
# Para adicionar um novo cliente:
# 1. Acesse o gtarp-manager
# 2. Crie um novo Tenant com as credenciais Discord do cliente
# 3. Configure o subdomínio correspondente na Vercel
# ==========================================
